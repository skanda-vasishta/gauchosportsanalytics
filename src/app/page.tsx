import { Column, Text } from "@/once-ui/components";

export default function Home() {
  return (
    <Column maxWidth="l" gap="xl" horizontal="center" vertical="center" style={{ minHeight: '100vh' }}>
      <Text variant="display-default-m">Redirecting to About page...</Text>
    </Column>
  );
}
